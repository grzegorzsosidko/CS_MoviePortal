@model IEnumerable<Firma.Data.Data.Movie.Gatunek>

@{
    ViewData["Title"] = "Gatunki filmowe";
    int rowNumber = 1;
}

<div class="card shadow-lg border-0 col-md-8 mx-auto">
    <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
        <h2 class="mb-0">
            <i class="fa-solid fa-tags me-2"></i>@ViewData["Title"]
        </h2>
        <a asp-action="Create" class="btn btn-success">
            <i class="fa-solid fa-plus-circle me-2"></i>Dodaj Nowy Gatunek
        </a>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-bordered mb-0">
                <thead class="thead-dark">
                    <tr class="text-center">
                        <th style="width: 10%;">LP</th>
                        <th>Nazwa gatunku</th>
                        <th style="width: 25%;">Akcje</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="3" class="text-center py-4">
                                <h4 class="text-muted">Brak gatunków w bazie danych.</h4>
                                <p>Kliknij przycisk "Dodaj Nowy Gatunek", aby dodać pierwszy.</p>
                            </td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var item in Model)
                        {
                            <tr class="align-middle">
                                <td class="text-center fw-bold">@(rowNumber++)</td>
                                <td class="ps-3">@Html.DisplayFor(modelItem => item.Name)</td>
                                <td class="text-center">
                                    <div class="btn-group" role="group">
                                        <a asp-action="Edit" asp-route-id="@item.IdGatunek" class="btn btn-primary" title="Edytuj">
                                            <i class="fa-solid fa-pencil-alt me-1"></i> Edytuj
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.IdGatunek" class="btn btn-danger" title="Usuń">
                                            <i class="fa-solid fa-trash-alt me-1"></i> Usuń
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
